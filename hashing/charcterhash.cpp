#include <iostream>
using namespace std;

/* int main() {
    int hash[256] = {0}; // Initialize an array of size 256 to store character frequencies

    string inputString;
    cout << "Enter a string: ";
    getline(cin, inputString); // Read a string with spaces

    // Calculate character frequencies
    for (char c : inputString) {
        hash[(int)c]++; // Increment the count for the ASCII value of the character
    }

    // Display character frequencies
    for (int i = 0; i < 256; i++) {
        if (hash[i] > 0) {
            cout << "Character '" << char(i) << "' appears " << hash[i] << " times." << endl;
        }
    }

    return 0;
} */
